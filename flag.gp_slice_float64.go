///////////////////////////////////////////////////////////////////
//
// !!!!!!!!!!!! NEVER MODIFY THIS FILE MANUALLY !!!!!!!!!!!!
//
// This file was auto-generated by tool [github.com/gxlb/gogp]
// Last update at: [Sat Feb 20 2021 16:19 CST]
// Generate from:
//   [github.com/gxlb/cli/internal/gp/flag.gp]
//   [github.com/gxlb/cli/flag.gpg] [flag_float64_slice]
//
// Tool [github.com/gxlb/gogp] info:
// CopyRight 2021 @Ally Dale. All rights reserved.
// Author  : Ally Dale(vipally@gmail.com)
// Site    : https://github.com/vipally
// Version : 3.0.0.final
// 
///////////////////////////////////////////////////////////////////

package cli

import (
	"flag"
	"fmt"
	"strconv"
	//"encoding/json"
	//"strings"
)

var _ = (*strconv.NumError)(nil) //avoid compile error

// Float64SliceSlice wraps []float64 to satisfy flag.Value
type Float64SliceSlice struct {
	slice      []float64
	hasBeenSet bool
}

// NewFloat64SliceSlice makes an *Float64SliceSlice with default values
func NewFloat64SliceSlice(defaults ...float64) *Float64SliceSlice {
	return &Float64SliceSlice{slice: append([]float64{}, defaults...)}
}

// clone allocate a copy of self object
func (s *Float64SliceSlice) clone() *Float64SliceSlice {
	n := &Float64SliceSlice{
		slice:      make([]float64, len(s.slice)),
		hasBeenSet: s.hasBeenSet,
	}
	copy(n.slice, s.slice)
	return n
}

// TODO: Consistently have specific Set function for Int64 and Float64 ?
// Append directly adds an integer to the list of values
func (s *Float64SliceSlice) Append(value ...float64) {
	if !s.hasBeenSet {
		s.slice = []float64{}
		s.hasBeenSet = true
	}

	s.slice = append(s.slice, value...)
}

// Set parses the value into an integer and appends it to the list of values
func (s *Float64SliceSlice) Set(value string) error {
	if !s.hasBeenSet {
		s.slice = []float64{}
		s.hasBeenSet = true
	}

	// if strings.HasPrefix(value, slPfx) {
	// 	// Deserializing assumes overwrite
	// 	_ = json.Unmarshal([]byte(strings.Replace(value, slPfx, "", 1)), &s.slice)
	// 	s.hasBeenSet = true
	// 	return nil
	// }

	tmp, err := strconv.ParseFloat(value, 64)
	if err != nil {
		return err
	}

	s.slice = append(s.slice, float64(tmp))

	return nil
}

// String returns a readable representation of this value (for usage defaults)
func (s *Float64SliceSlice) String() string {
	return fmt.Sprintf("%#v", s.slice)
}

// Serialize allows Float64SliceSlice to fulfill Serializer
func (s *Float64SliceSlice) Serialize() string {
	//TODO:
	// jsonBytes, _ := json.Marshal(s.slice)
	// return fmt.Sprintf("%s%s", slPfx, string(jsonBytes))
	return ""
}

// Value returns the slice of ints set by this flag
func (s *Float64SliceSlice) Value() []float64 {
	return s.slice
}

// Get returns the slice set by this flag
func (s *Float64SliceSlice) Get() interface{} {
	return *s
}

// Float64SliceFlag define a value of type *Float64SliceSlice
type Float64SliceFlag struct {
	////////////////////////////////////////////////////////////////////////////
	//
	//name related area
	//
	LogicName string   // logic name of the flag
	Name      string   // name of the flag
	Aliases   []string // aliases of the flag
	Usage     string   // usage string
	Required  bool     // if required
	Hidden    bool     // hidden this flag
	EnvVars   []string // environment values
	FilePath  string   // file path
	//
	//value related area
	//
	Target      *Float64SliceSlice // Target value pointer outside
	Default     *Float64SliceSlice // Default value
	DefaultText string             // Default value in help info
	Enums       []float64          // Enumeration of valid values
	Ranges      []float64          // {[min,max),[min,max),[min...)} ranges of valid values

	////////////////////////////////////////////////////////////////////////////
	//area for parsing
	value *Float64SliceSlice // value that affect by flagset
	info  FlagInfo           // parsed info of this flag
}

// Init verify and init the value by ower flag
func (v *Float64SliceFlag) Init() error {
	v.info.Flag = v
	if l := len(v.Enums); l > maxSliceLen {
		return fmt.Errorf("flag %s.Enums too long: %d/%d", v.info.LogicName, l, maxSliceLen)
	}
	if l := len(v.Ranges); l > maxSliceLen {
		return fmt.Errorf("flag %s.Ranges too long: %d/%d", v.info.LogicName, l, maxSliceLen)
	}
	if err := v.validateValues(v.Default); err != nil {
		return fmt.Errorf("default value invalid: %s", err.Error())
	}
	return nil
}

// IsSet check if value was set
func (v *Float64SliceFlag) IsSet() bool {
	return v.value.hasBeenSet
}

// Apply coordinate the value to flagset
func (v *Float64SliceFlag) Apply(set *flag.FlagSet) error {
	return nil
}

// String return the value for view
func (v *Float64SliceFlag) String() string {
	return ""
}

// ValidateValues verify if all values was valid
func (v *Float64SliceFlag) ValidateValues() error {
	return v.validateValues(v.value)
}

// Info returns parsed info of this flag
func (v *Float64SliceFlag) Info() *FlagInfo {
	return &v.info
}

// for default value verify
func (v *Float64SliceFlag) validateValues(values *Float64SliceSlice) error {
	for _, val := range values.slice {
		if err := v.validValue(val); err != nil {
			return err
		}
	}
	return nil
}

// check if value if valid for this flag
func (v *Float64SliceFlag) validValue(value float64) error {
	f := &v.info
	if len(v.Enums) > 0 {
		found := false
		for _, v := range v.Enums {
			if value == v {
				found = true
				break
			}
		}
		if !found {
			return fmt.Errorf("flag %s value %v out of Enums: %v", f.LogicName, value, v.Enums)
		}
	}
	if len(v.Ranges) > 0 {
		found := false
		for i := 0; i < len(v.Ranges); i++ {
			min := v.Ranges[i]
			max := min
			if i++; i < len(v.Ranges) {
				max = v.Ranges[i]
			}
			if value >= min && value < max {
				found = true
				break
			}
		}
		if !found {
			return fmt.Errorf("flag %s value %v out of Ranges: %v", f.LogicName, value, v.Enums)
		}
	}
	return nil
}

var _ Flag = (*Float64SliceFlag)(nil) //for interface verification only
